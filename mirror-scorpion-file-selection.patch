From 0848c1985ef6b75ebe5daad771346e495e5ca0ef Mon Sep 17 00:00:00 2001
From: magdymeko456-cell
 <2.88837127e+08+magdymeko456-cell@users.noreply.github.com>
Date: Sun, 23 Aug 2026 20:18:01 +0000
Subject: [PATCH] feat(files): add device audio and PDF selection

---
 docs/google-audio-transcription-gate.md | 34 ++++++++++++++++++
 lib/features/feature_hub_screen.dart    | 48 +++++++++++++++++++++----
 pubspec.yaml                            |  1 +
 3 files changed, 77 insertions(+), 6 deletions(-)
 create mode 100644 docs/google-audio-transcription-gate.md

diff --git a/docs/google-audio-transcription-gate.md b/docs/google-audio-transcription-gate.md
new file mode 100644
index 0000000..45f01d3
--- /dev/null
+++ b/docs/google-audio-transcription-gate.md
@@ -0,0 +1,34 @@
+# بوابة تفريغ ملفات الصوت عبر Google
+
+## قرار التنفيذ
+
+لا يمكن ربط دبوس ملف الصوت في Flutter بترجمة حقيقية من دون مسار خادمي. المسار المقترح هو رفع اختياري إلى مساحة تخزين خاصة، ثم استدعاء دالة خادمية تتحقق من نوع الملف والحجم والملكية، وتطلق تفريغ Google Speech-to-Text، ثم تمرر النص الناتج إلى خدمة الترجمة وتعيد حالة عملية قابلة للمتابعة. لا تُحفظ مفاتيح Google أو حسابات خدمة في التطبيق أو المستودع.
+
+## قيود معمارية مؤكدة
+
+| الحالة | المسار الصحيح | أثر تجربة المستخدم |
+|---|---|---|
+| ملف أقصر من 60 ثانية | تفريغ متزامن عندما يكون الحجم والصيغة مدعومين. | نتيجة أو خطأ واضح في جلسة واحدة. |
+| ملف أطول من 60 ثانية | رفع إلى Cloud Storage ثم Batch Recognition غير متزامن. | يظهر رقم عملية وحالة معالجة؛ لا يُعرض «جارٍ الآن» بصورة مضللة. |
+| نموذج التفريغ | `chirp_3` مع لغة الملف التي يختارها المستخدم أو كشف لغة بموافقة واضحة. | تذكر الواجهة أن الدقة تعتمد على التسجيل واللغة المختارة. |
+| الاحتفاظ بالملف | حذف الإدخال والناتج تلقائياً بعد مدة معلنة أو حذف المستخدم. | لا تُحتفظ ملفات الصوت إلى أجل غير محدد. |
+
+## متطلبات لا يمكن تجاوزها
+
+يتطلب التنفيذ الحي تفعيل واجهة Speech-to-Text، تفعيل الفوترة، إعداد Cloud Storage بصلاحيات محدودة، ونشر Cloud Functions. لأن ذلك قد ينشئ تكاليف معالجة وتخزين، فلا يُفعّل تلقائياً من الكود. تعد تكلفة STT V2 القياسية منشورة بحسب دقائق الصوت المعالج، وتوجد معالجة Batch ديناميكية أقل سعراً وأعلى زمناً. كذلك تتطلب الملفات الطويلة تخزيناً سحابياً ومعالجة غير متزامنة.
+
+## حدود النسخة الحالية
+
+زر الدبوس الحالي في محرر المصدر صحيح المكان، لكنه يعرض حالة عدم توفر صادقة. لن يتحول إلى تفريغ/ترجمة ملف حتى تتحقق متطلبات هذه الوثيقة وتُختبر على Android.
+
+## اختيار الملفات على الجهاز
+
+يعتمد التطبيق منتقي ملفات النظام عبر حزمة `file_picker` الرسمية. يسمح ذلك باختيار ملف صوت أو PDF من مساحة المستخدم أو موفر ملفات سحابي ظاهر في نظام Android، مع تقييد الامتدادات من الواجهة. لا يعني اختيار الملف أنه رُفع أو فُرّغ أو حُفظ؛ لا يبدأ النقل إلا بعد موافقة صريحة ومسار التخزين الخادمي المذكور أعلاه.
+
+## مراجع رسمية
+
+1. [تفريغ الملفات الطويلة غير المتزامن — Google Cloud Speech-to-Text](https://docs.cloud.google.com/speech-to-text/docs/batch-recognize)
+2. [اختيار نموذج التفريغ — Google Cloud Speech-to-Text](https://docs.cloud.google.com/speech-to-text/docs/transcription-model)
+3. [ترميزات الصوت المدعومة — Google Cloud Speech-to-Text](https://docs.cloud.google.com/speech-to-text/docs/encoding)
+4. [تسعير Speech-to-Text — Google Cloud](https://cloud.google.com/speech-to-text/pricing)
+5. [منتقي الملفات الرسمي لـ Flutter](https://pub.dev/packages/file_picker)
diff --git a/lib/features/feature_hub_screen.dart b/lib/features/feature_hub_screen.dart
index a4467ce..1ab7ca1 100644
--- a/lib/features/feature_hub_screen.dart
+++ b/lib/features/feature_hub_screen.dart
@@ -2,6 +2,7 @@ import 'dart:async';
 
 import 'package:flutter/material.dart';
 import 'package:flutter/services.dart';
+import 'package:file_picker/file_picker.dart';
 import 'package:image_picker/image_picker.dart';
 import 'package:provider/provider.dart';
 
@@ -173,9 +174,15 @@ class _TranslationPanelState extends State<_TranslationPanel> {
     }
   }
 
-  void _audioUploadUnavailable() {
+  Future<void> _pickAudioFile() async {
+    final file = await FilePicker.pickFile(type: FileType.audio);
+    if (!mounted) return;
+    if (file == null) {
+      setState(() => _notice = 'لم يتم اختيار ملف صوت.');
+      return;
+    }
     setState(() {
-      _notice = 'اختيار الملف موجود في محرر المصدر، لكن تفريغ الملفات وترجمتها لم يُربطا بعد بخدمة صوت حية؛ لن يُنتج التطبيق ترجمة وهمية.';
+      _notice = 'تم اختيار «${file.name}» محلياً. لن يُرفع أو يُفرّغ قبل تشغيل خدمة الصوت الحية وموافقتك الصريحة.';
     });
   }
 
@@ -323,7 +330,7 @@ class _TranslationPanelState extends State<_TranslationPanel> {
             _EditorAction(
               icon: Icons.attach_file,
               tooltip: 'اختيار ملف صوتي لترجمته',
-              onPressed: _audioUploadUnavailable,
+              onPressed: _pickAudioFile,
             ),
           ],
         ),
@@ -592,9 +599,13 @@ class _DialoguePanelState extends State<_DialoguePanel> {
     }
   }
 
-  void _audioFileUnavailable() {
+  Future<void> _pickDialogueAudioFile() async {
+    final file = await FilePicker.pickFile(type: FileType.audio);
+    if (!mounted) return;
     setState(() {
-      _notice = 'دبوس محرر المصدر جاهز لاختيار ملف صوت، لكن تفريغ الملف وترجمته يحتاجان خدمة صوت حية؛ لا توجد نتيجة بديلة مصطنعة.';
+      _notice = file == null
+          ? 'لم يتم اختيار ملف صوت.'
+          : 'تم اختيار «${file.name}» لمصدر الحوار. التفريغ والترجمة ينتظران خدمة صوت حية؛ لا توجد نتيجة بديلة مصطنعة.';
     });
   }
 
@@ -625,7 +636,7 @@ class _DialoguePanelState extends State<_DialoguePanel> {
             _EditorAction(
               icon: Icons.attach_file,
               tooltip: 'اختيار ملف صوت لمصدر الحوار',
-              onPressed: _audioFileUnavailable,
+              onPressed: _pickDialogueAudioFile,
             ),
           ],
           onChanged: (value) => _queueTranslation(
@@ -842,6 +853,7 @@ class _DocumentsPanelState extends State<_DocumentsPanel> {
   bool _isScanning = false;
   bool _isTranslating = false;
   String? _selectedFileName;
+  PlatformFile? _selectedPdf;
   String? _extractedText;
   String? _translatedText;
   String? _notice;
@@ -926,6 +938,20 @@ class _DocumentsPanelState extends State<_DocumentsPanel> {
     }
   }
 
+  Future<void> _pickPdf() async {
+    final file = await FilePicker.pickFile(
+      type: FileType.custom,
+      allowedExtensions: const ['pdf'],
+    );
+    if (!mounted) return;
+    setState(() {
+      _selectedPdf = file;
+      _notice = file == null
+          ? 'لم يتم اختيار PDF.'
+          : 'تم اختيار «${file.name}» محلياً. استخراج صفحات PDF وترجمتها مسار مستقل لم يُفعّل بعد؛ لن يعرض التطبيق نتيجة مصطنعة.';
+    });
+  }
+
   @override
   Widget build(BuildContext context) {
     return ListView(
@@ -977,6 +1003,16 @@ class _DocumentsPanelState extends State<_DocumentsPanel> {
             onTap: _isScanning ? null : () => _scanImage(ImageSource.gallery),
           ),
         ),
+        const SizedBox(height: 12),
+        Card(
+          child: ListTile(
+            leading: const Icon(Icons.picture_as_pdf_outlined, color: RoyalColors.gold),
+            title: const Text('اختيار مستند PDF'),
+            subtitle: Text(_selectedPdf == null ? 'اختيار محلي أولاً؛ الترجمة متعددة الصفحات لم تُفعّل بعد' : 'المستند المختار: ${_selectedPdf!.name}'),
+            trailing: const Icon(Icons.chevron_left),
+            onTap: _isScanning || _isTranslating ? null : _pickPdf,
+          ),
+        ),
         if (_selectedFileName != null)
           Padding(
             padding: const EdgeInsets.only(top: 18),
diff --git a/pubspec.yaml b/pubspec.yaml
index de95120..c133ed1 100644
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -17,6 +17,7 @@ dependencies:
   google_mlkit_translation: ^0.15.1
   google_mlkit_text_recognition: ^0.17.1
   image_picker: ^1.2.3
+  file_picker: ^12.0.0
   flutter_tts: ^4.2.5
   speech_to_text: ^7.4.0
 
-- 
2.43.0

